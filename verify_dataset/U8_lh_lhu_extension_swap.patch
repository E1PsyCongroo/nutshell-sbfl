--- a/src/main/scala/nutcore/backend/fu/UnpipelinedLSU.scala
+++ b/src/main/scala/nutcore/backend/fu/UnpipelinedLSU.scala
@@ -405,10 +405,10 @@ class LSExecUnit extends NutCoreModule {
   val rdataSel = if (XLEN == 32) rdataSel32 else rdataSel64
   val rdataPartialLoad = LookupTree(func, List(
       LSUOpType.lb   -> SignExt(rdataSel(7, 0) , XLEN),
-      LSUOpType.lh   -> SignExt(rdataSel(15, 0), XLEN),
+      LSUOpType.lh   -> ZeroExt(rdataSel(15, 0), XLEN),
       LSUOpType.lw   -> SignExt(rdataSel(31, 0), XLEN),
       LSUOpType.lbu  -> ZeroExt(rdataSel(7, 0) , XLEN),
-      LSUOpType.lhu  -> ZeroExt(rdataSel(15, 0), XLEN),
+      LSUOpType.lhu  -> SignExt(rdataSel(15, 0), XLEN),
       LSUOpType.lwu  -> ZeroExt(rdataSel(31, 0), XLEN)
   ))
   val addrAligned = LookupTree(func(1,0), List(
